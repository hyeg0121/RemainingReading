## 4.7. 도메인 구현과 DIP

- 실제 코드를 예로 들었을 때, OrderRepositoty는 Jpa의 Repository를 상속하고 있으므로 도메인이 인프라에 의존하는 것임
- 하지만 Jpa Repository를 상속하는 InfraRepository를 직접 구현하고, 그 인터페이스를 다른 도메인 리포지토리에서 구현하게 되면, DIP를 적용하게 되는 것
	- JpaRepository가 바뀌었을 때, InfraRepository만 수정해주면 되기 때문에
- 근데 이럴거면 왜 프레임워크를 쓰는 겨…? 현재로서는 별로 좋아보이는 방법은 아니나 확실히 알아두면 좋긴 할 듯